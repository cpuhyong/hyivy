{
	"name": "SQL script 2",
	"properties": {
		"content": {
			"query": "--在无服务器 SQL 池中创建临时表的示例语句\n--要一起跑才行，不然一会话结束，啥都没有，可以用来算ODSTDS合并的结果哦，哈哈，新思路\nCREATE TABLE #MyTempTable (\n    ID INT,\n    CustomerName NVARCHAR(100),\n    OrderDate DATE,\n    SalesAmount DECIMAL(10, 2)\n);\n\n\nINSERT INTO #MyTempTable (ID, CustomerName, OrderDate, SalesAmount)\nVALUES (1, '示例客户', '2024-01-22', 299.99);\n\n\n\nSELECT * FROM #MyTempTable;\n\n\n\nCREATE EXTERNAL TABLE ods_order_info3 (\n    id                     BIGINT,\n    consignee              VARCHAR(100),\n    consignee_tel          VARCHAR(20),\n    total_amount           DECIMAL(10,2),\n    order_status           VARCHAR(20),\n    user_id                BIGINT,\n    payment_way            VARCHAR(20),\n    delivery_address       VARCHAR(1000),\n    order_comment          VARCHAR(200),\n    out_trade_no           VARCHAR(50),\n    trade_body             VARCHAR(200),\n    create_time            DATETIME2,\n    operate_time           DATETIME2,\n    expire_time            DATETIME2,\n    process_status         VARCHAR(20),\n    tracking_no            VARCHAR(100),\n    parent_order_id        BIGINT,\n    img_url                VARCHAR(200),\n    province_id            INT,\n    activity_reduce_amount DECIMAL(16,2),\n    coupon_reduce_amount   DECIMAL(16,2),\n    original_total_amount  DECIMAL(16,2),\n    feight_fee             DECIMAL(16,2),\n    feight_fee_reduce      DECIMAL(16,2),\n    refundable_time        DATETIME2--,\n    --CONSTRAINT PK_ods_order_info PRIMARY KEY NONCLUSTERED (id) NOT ENFORCED  -- 定义主键\n)\nWITH (\n    LOCATION = 'ods_order_info3/',\n    DATA_SOURCE = ods_external_source,\n    FILE_FORMAT = parquet_format\n);\n\nselect * from tds_order_info\n\nselect * from ods_order_info1 order by id--316\n\nselect * from ods_order_info2 order by id--200\n\nselect * from ods_order_info3 order by id--316",
			"metadata": {
				"language": "sql"
			},
			"currentConnection": {
				"databaseName": "gmall",
				"poolName": "Built-in"
			},
			"resultLimit": 5000
		},
		"type": "SqlQuery"
	}
}